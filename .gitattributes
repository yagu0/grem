# Strip notebook outputs in the Git index on commit (working tree unchanged).
# Requires: pip install nbstripout, then once per clone: nbstripout --install
*.ipynb filter=nbstripout diff=ipynb
*.zpln filter=nbstripout
