répulsion sur arêtes ?
cherry sur sommet : faire apparaitre enfants proche du sommet parent
répulsion arête sommet : calcul distances sommet à arête pour empêcher de traverser

sommets en couleur affichage dynamique dans ordre d'apparition (boucle ?)

pal = ig.drawing.colors.RainbowPalette(n=100)
ig.plot(
    graph,
    layout=coords,
    vertex_size=10,
    vertex_color=[pal.get(i) for i in range(100)],
......
